#include<stdio.h>
int main()
{
	int a[20],e=0,o=0,n,i;
	printf("enter size");
	scanf("%d",&n);
	printf("enter numbers");
	for(i=0; i<n; i++)
 	{
	
	  scanf("%d",&a[i]);
    }
  for(i=0;i<n;i++)
  {
  if(a[i]%2==0)
     e++;
 
  //else
  //{ 
 // o++;
 // } 
   }
   printf("no of evens=%d\n",e);
   printf("no of odds=%d",n-e);
   return 0;
   
}
